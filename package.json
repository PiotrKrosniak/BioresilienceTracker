{
  "name": "biotracker",
  "version": "1.0.0",
  "description": "BioTracker application with Netlify functions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "netlify dev",
    "start": "netlify dev"
  },
  "dependencies": {
    "googleapis": "^150.0.1",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
